---
interface Props {
    url: string | undefined
    title: string
    lastUpdated: string
}
const {url, title, lastUpdated}: Props = Astro.props
---
<li>
    <a href={url}>{ `${ title } ultima actualización: ${lastUpdated}` }</a>
</li>